module.exports = {
  transpileDependencies: true,
  devServer: {
    open: true,      // Abrir navegador al iniciar el servidor
    port: 8080,      // Puerto en el que correrá la app
    hot: true        // Habilitar recarga en caliente
  }
};
